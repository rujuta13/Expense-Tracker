CREATE DATABASE EXPENSETRACKER
USE EXPENSETRACKER;
CREATE TABLE USER (ID INT PRIMARY KEY NOT NULL,
         UNAME TEXT NOT NULL,
        EMAIL TEXT NOT NULL,
        PWD TEXT NOT NULL);

INSERT INTO USER (ID, UNAME, EMAIL, PWD) VALUES (1, 'rujutab', 'rujuta@mail.com', 'qwerty');
INSERT INTO USER (ID, UNAME, EMAIL, PWD) VALUES (2, 'pritip', 'priti@mail.com', '12345');